# ============================================
# Photo to Video AI - 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요
# cp .env.sample .env
# ============================================

# ============================================
# Cloud AI - Stability AI (권장)
# ============================================
# 사용 모델: Stable Video Diffusion (SVD)
# API: Image-to-Video v2beta
# 가격: $0.20 / 영상 (약 4초)
# API 키 발급: https://platform.stability.ai/account/keys
#
# 발급 방법:
# 1. https://platform.stability.ai/ 접속
# 2. 계정 생성 또는 로그인
# 3. 좌측 메뉴 "API Keys" 클릭
# 4. "Create API Key" 클릭
# 5. 생성된 키를 아래에 붙여넣기
STABILITY_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# 데이터베이스 (Docker 사용 시 기본값 그대로)
# ============================================
POSTGRES_USER=photouser
POSTGRES_PASSWORD=photopass
POSTGRES_DB=photovideo
DATABASE_URL=postgresql://photouser:photopass@localhost:5432/photovideo

# ============================================
# Redis (Docker 사용 시 기본값 그대로)
# ============================================
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ============================================
# MinIO / S3 스토리지 (Docker 사용 시 기본값 그대로)
# ============================================
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=photo-to-video
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# ============================================
# Local AI (GPU 모드) - 선택사항
# ============================================
# NVIDIA GPU 8GB+ VRAM 필요
# 모델: stabilityai/stable-video-diffusion-img2vid-xt-1-1
#
# 사용하려면:
# 1. python scripts/download_models.py --model svd-xt-1.1
# 2. 아래 설정 활성화
ENABLE_LOCAL_MODELS=false
LOCAL_MODEL_PATH=./models/svd-xt-1.1
GPU_DEVICE=cuda:0
DEFAULT_LOCAL_MODEL=svd-xt-1.1

# ============================================
# 프론트엔드 URL (개발 시 기본값 그대로)
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
FRONTEND_URL=http://localhost:3000

# ============================================
# 애플리케이션 보안
# ============================================
# 프로덕션에서는 반드시 변경하세요!
SECRET_KEY=change-this-in-production-use-random-string

# ============================================
# 기타 AI API 키 (미구현, 향후 지원 예정)
# ============================================
RUNWAY_API_KEY=
PIKA_API_KEY=
